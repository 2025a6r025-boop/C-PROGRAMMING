#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int rows,cols;
    scanf("%d %d",&rows,&cols);
    int arr[rows][cols];
    int T[cols][rows];
    for(int i=0;i<rows;i++){
        for(int j=0;j<cols;j++){
            scanf("%d",&arr[i][j]);
        }
    }
    for(int i=0;i<rows;i++){
        for(int j=0;j<cols;j++){
            T[j][i]=arr[i][j];
        }
    }
    for(int i=0;i<cols;i++){
        for(int j=0;j<rows;j++){
            printf("%d ",T[i][j]);
        }
        printf("\n");
    }
    return 0;
}
