#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int x;
    int y;
    int *ptr1=&x;
    int *ptr2=&y;
    scanf("%d %d",ptr1,ptr2);
      int temp=*ptr1;
    *ptr1=*ptr2;
    *ptr2=temp;
    printf("%d %d",*ptr1,*ptr2);
    return 0;
}
