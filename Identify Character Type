#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   char ch;
    scanf("%c",&ch);
    if((ch>=65 && ch<=90) || (ch>=97 && ch<=122)){
        printf("Alphabet",ch);
    }
    else if (ch>=48 && ch<=57){
        printf("Digit",ch);
    }
    else{
        printf("Symbol",ch);
    }
        
    return 0;
}



#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <ctype.h>

int main() {

    char ch;
    scanf("%c", &ch);
    if(isalpha(ch)){
        printf("Alphabet");
    }
    else if(isdigit(ch)){
        printf("Digit");
    }
    else {
        printf("Symbol");
    }
    return 0;
}
