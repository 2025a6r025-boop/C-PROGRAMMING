#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int isPrime(int num){
    int count=0;
    if(num<2)
        return 0;
    for(int i=1;i<=num;i++){
        if(num%i==0)
        count++;
    }
    if(count==2)
        return 1;
    else 
        return 0;
}
int main(){
    int start,end;
    int found=0;
    scanf("%d %d",&start,&end);
    for(int i=start;i<=end;i++){
        if(isPrime(i)){
            printf("%d ",i);
            found=1;
        }
    }
    if(!found)
        printf("No prime numbers found");
    return 0;
}
